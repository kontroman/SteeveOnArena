Building signs receive a 5 x 5 world-unit ground outline (2.5 blocks from center to each edge) through BuildingZone.Awake. The 0.18-unit-wide line is uniform gold, with 70% opacity and gentle brightness pulsation. The unlit shader uses an HDR intensity of 1.5 for a luminous appearance; a soft 0.28-unit halo is rendered on both sides of the line and works without camera bloom. The visual outline breathes between 97% and 103% scale around its own center with a period of about 2.09 seconds; the construction trigger stays fixed at 5 x 5 units. Color, glow intensity and opacity are adjustable on Assets/Resources/BuildingSignOutline.mat.

Original texture: Assets/Textures/BuildingSignOutline.png. Generated with the built-in image_gen tool and retained as a source asset. The current solid-color material does not sample a texture.

Generation prompt:
Use case: stylized-concept. Asset type: seamless tileable pixel-art texture for an animated building-placement boundary line in a Minecraft-style Unity game. Generate a square flat texture filled edge to edge with chunky, crisp square pixels, pale cream and warm golden yellow, a simple repeating stepped dash pattern flowing horizontally. Very low detail, equivalent to a 16x16 pixel game texture enlarged with nearest-neighbor. Opaque, seamless on both axes. No perspective, no objects, no text, no borders, no shadows, no gradients. This texture will be mapped onto a thin square ground outline, not displayed as an illustration.

The outline lies horizontally at the bottom of the sign renderer bounds plus 0.035 units. Intended for the flat village building sites; does not conform to uneven terrain. BuildingZone fits its existing BoxCollider trigger to the same ground center and shared radius at startup (5 x 5 units, 2 units high), compensating for zone scale. Existing zones are aligned with world axes. Entering this trigger opens the building window subject to the existing tutorial rules; exiting closes it. DestroySign restores the original trigger dimensions for built-building interactions. Verify entry and exit on all four sides in Unity Play Mode, then build a building and confirm both sign and outline disappear.
